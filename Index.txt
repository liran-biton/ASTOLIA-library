<!DOCTYPE html>
<html>
<head>
  <base target="_top">
  <meta charset="UTF-8">
  <title>ASTOLIA library</title>
  <style>
    :root{
      --cream:      #F5F2EC;
      --white:      #FFFFFF;
      --sand:       #E1D9C8;
      --sand-line:  #DDD6C8;
      --sage:       #8FA894;
      --sage-dark:  #5F7A67;
      --teal-deep:  #2E4A44;
      --teal-mid:   #3F5B52;
      --navy:       #26415E;
      --gold:       #C9A46A;
      --text-dark:  #33413D;
      --text-mute:  #7C8783;
      --danger:     #B4553F;
      --radius:     14px;
      --shadow:     0 4px 18px rgba(46,74,68,0.08);
    }

    *{box-sizing:border-box;}
    html,body{
      margin:0;padding:0;height:100%;
      background:var(--cream);
      font-family:'Segoe UI', Tahoma, Georgia, sans-serif;
      color:var(--text-dark);
    }
    button{font-family:inherit;cursor:pointer;}
    input,select,textarea{font-family:inherit;}

    /* ---------- LOGIN SCREEN ---------- */
    #loginScreen{
      min-height:100vh;
      display:flex;align-items:center;justify-content:center;
      background:
        radial-gradient(circle at 20% 20%, rgba(143,168,148,0.18), transparent 45%),
        radial-gradient(circle at 80% 80%, rgba(201,164,106,0.15), transparent 45%),
        var(--cream);
      padding:20px;
    }
    .login-card{
      width:100%;max-width:400px;
      background:var(--white);
      border:1px solid var(--sand-line);
      border-radius:var(--radius);
      box-shadow:var(--shadow);
      padding:40px 36px;
      text-align:center;
    }
    .brand{
      font-size:28px;
      letter-spacing:2px;
      color:var(--teal-deep);
      font-weight:700;
      margin-bottom:2px;
    }
    .brand span{font-weight:300;color:var(--sage-dark);}
    .brand-sub{
      font-size:12px;letter-spacing:3px;text-transform:uppercase;
      color:var(--gold);margin-bottom:30px;
    }
    .field-label{
      text-align:left;font-size:13px;color:var(--text-mute);
      margin:16px 0 6px;font-weight:600;
    }
    .input{
      width:100%;padding:12px 14px;
      border:1px solid var(--sand-line);
      border-radius:10px;font-size:15px;
      background:var(--cream);color:var(--text-dark);
      outline:none;transition:.15s;
    }
    .input:focus{border-color:var(--sage-dark);background:var(--white);}
    .btn{
      width:100%;padding:12px 14px;border:none;border-radius:10px;
      font-size:15px;font-weight:600;margin-top:20px;
      background:var(--teal-deep);color:var(--white);
      transition:.15s;
    }
    .btn:hover{background:var(--teal-mid);}
    .btn:disabled{opacity:.55;cursor:default;}
    .btn-secondary{
      background:transparent;color:var(--sage-dark);
      border:1px solid var(--sand-line);margin-top:10px;
    }
    .btn-secondary:hover{background:var(--sand);}
    .msg{font-size:13px;margin-top:14px;min-height:16px;}
    .msg.error{color:var(--danger);}
    .msg.success{color:var(--sage-dark);}
    .hidden{display:none !important;}
    .spinner{
      display:inline-block;width:14px;height:14px;
      border:2px solid rgba(255,255,255,.5);border-top-color:#fff;
      border-radius:50%;animation:spin .7s linear infinite;
      vertical-align:middle;margin-right:6px;
    }
    @keyframes spin{to{transform:rotate(360deg);}}

    /* ---------- APP SHELL ---------- */
    #appShell{display:none;min-height:100vh;}
    #appShell.active{display:flex;}

    .sidebar{
      width:240px;flex-shrink:0;
      background:var(--teal-deep);
      color:var(--cream);
      display:flex;flex-direction:column;
      padding:26px 0;
    }
    .sidebar-brand{
      padding:0 24px 26px;border-bottom:1px solid rgba(255,255,255,0.1);margin-bottom:18px;
    }
    .sidebar-brand .brand{color:var(--white);font-size:20px;text-align:left;margin:0;}
    .sidebar-brand .brand-sub{color:var(--gold);text-align:left;margin:2px 0 0;}

    .nav-item{
      display:flex;align-items:center;gap:10px;
      padding:13px 24px;font-size:14px;font-weight:500;
      color:rgba(245,242,236,0.75);cursor:pointer;
      border-left:3px solid transparent;
      transition:.15s;
    }
    .nav-item:hover{background:rgba(255,255,255,0.06);color:var(--white);}
    .nav-item.active{
      background:rgba(255,255,255,0.09);
      border-left-color:var(--gold);
      color:var(--white);
    }
    .nav-dot{width:6px;height:6px;border-radius:50%;background:var(--sage);flex-shrink:0;}

    .sidebar-footer{
      margin-top:auto;padding:16px 24px 0;border-top:1px solid rgba(255,255,255,0.1);
    }
    .user-chip{font-size:13px;color:var(--white);font-weight:600;}
    .user-role{font-size:11px;color:var(--sage);text-transform:uppercase;letter-spacing:1px;margin-top:2px;}
    .logout-link{
      display:inline-block;margin-top:12px;font-size:12px;color:rgba(245,242,236,0.6);
      text-decoration:underline;cursor:pointer;
    }
    .logout-link:hover{color:var(--white);}

    /* ---------- MAIN CONTENT ---------- */
    .main{flex:1;padding:32px 40px;overflow-y:auto;}
    .page-header{
      display:flex;align-items:center;justify-content:space-between;
      margin-bottom:24px;flex-wrap:wrap;gap:14px;
    }
    .page-title{font-size:24px;font-weight:700;color:var(--teal-deep);}
    .page-title small{display:block;font-size:12px;font-weight:500;color:var(--text-mute);margin-top:4px;letter-spacing:.5px;}

    .btn-accent{
      background:var(--gold);color:var(--white);border:none;
      padding:11px 20px;border-radius:10px;font-weight:600;font-size:14px;
      display:inline-flex;align-items:center;gap:6px;
    }
    .btn-accent:hover{background:#B8935A;}

    /* filter bar */
    .filter-bar{
      display:flex;gap:12px;flex-wrap:wrap;
      background:var(--white);border:1px solid var(--sand-line);
      border-radius:var(--radius);padding:16px 18px;margin-bottom:26px;
      box-shadow:var(--shadow);
    }
    .filter-bar .input, .filter-bar select{
      flex:1;min-width:160px;
    }
    .filter-bar select{
      padding:12px 14px;border:1px solid var(--sand-line);border-radius:10px;
      background:var(--cream);color:var(--text-dark);
    }

    /* card grid */
    .grid{
      display:grid;grid-template-columns:repeat(auto-fill, minmax(270px,1fr));
      gap:20px;
    }
    .creator-card{
      background:var(--white);border:1px solid var(--sand-line);
      border-radius:var(--radius);padding:22px;box-shadow:var(--shadow);
      display:flex;flex-direction:column;gap:10px;
      transition:.15s;
    }
    .creator-card:hover{transform:translateY(-2px);box-shadow:0 8px 22px rgba(46,74,68,0.12);}
    .avatar-ring{
      width:52px;height:52px;border-radius:50%;
      background:linear-gradient(135deg, var(--sage), var(--teal-mid));
      color:var(--white);display:flex;align-items:center;justify-content:center;
      font-weight:700;font-size:18px;
    }
    .creator-name{font-size:16px;font-weight:700;color:var(--teal-deep);}
    .creator-city{font-size:12px;color:var(--gold);font-weight:600;text-transform:uppercase;letter-spacing:.5px;}
    .creator-about{font-size:13px;color:var(--text-mute);line-height:1.5;
      display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden;}
    .creator-contact{font-size:12.5px;color:var(--text-dark);}
    .social-row{display:flex;gap:10px;margin-top:4px;}
    .social-link{
      font-size:12px;color:var(--navy);text-decoration:none;
      background:var(--cream);border:1px solid var(--sand-line);
      padding:5px 10px;border-radius:20px;
    }
    .social-link:hover{background:var(--sand);}
    .card-footer{margin-top:auto;padding-top:10px;border-top:1px solid var(--sand-line);
      display:flex;justify-content:flex-end;}
    .edit-btn{
      font-size:12.5px;font-weight:600;color:var(--sage-dark);
      background:none;border:none;text-decoration:underline;
    }
    .empty-state{
      text-align:center;padding:60px 20px;color:var(--text-mute);
      background:var(--white);border:1px dashed var(--sand-line);border-radius:var(--radius);
    }

    /* coming soon */
    .coming-soon{
      background:var(--white);border:1px solid var(--sand-line);border-radius:var(--radius);
      padding:80px 30px;text-align:center;box-shadow:var(--shadow);
    }
    .coming-soon h3{color:var(--teal-deep);font-size:20px;margin-bottom:8px;}
    .coming-soon p{color:var(--text-mute);font-size:14px;}
    .badge-soon{
      display:inline-block;margin-top:14px;font-size:11px;font-weight:700;
      letter-spacing:1.5px;text-transform:uppercase;color:var(--white);
      background:var(--sage-dark);padding:6px 14px;border-radius:20px;
    }

    /* modal */
    .modal-overlay{
      display:none;position:fixed;inset:0;background:rgba(46,74,68,0.45);
      align-items:center;justify-content:center;padding:20px;z-index:100;
    }
    .modal-overlay.active{display:flex;}
    .modal{
      background:var(--white);border-radius:var(--radius);
      width:100%;max-width:520px;max-height:90vh;overflow-y:auto;
      padding:28px 30px;box-shadow:0 20px 60px rgba(0,0,0,0.25);
    }
    .modal h3{color:var(--teal-deep);margin-top:0;}
    .modal-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;}
    .modal-grid .full{grid-column:1 / -1;}
    .modal-actions{display:flex;gap:10px;margin-top:22px;}
    .modal-actions .btn{margin-top:0;}
    textarea.input{resize:vertical;min-height:70px;}

    .nav-badge{
      margin-left:auto;background:var(--gold);color:var(--white);
      font-size:11px;font-weight:700;padding:2px 8px;border-radius:10px;
    }

    /* sub-tabs (inside Creator Partnerships) */
    .sub-tabs{display:flex;gap:6px;margin-bottom:22px;border-bottom:1px solid var(--sand-line);}
    .sub-tab{
      padding:10px 18px;font-size:13.5px;font-weight:600;color:var(--text-mute);
      cursor:pointer;border-bottom:2px solid transparent;
    }
    .sub-tab:hover{color:var(--teal-deep);}
    .sub-tab.active{color:var(--teal-deep);border-bottom-color:var(--gold);}
    .subtab-page.hidden{display:none;}

    .btn-secondary-sm{
      background:transparent;border:1px solid var(--sand-line);color:var(--sage-dark);
      padding:11px 16px;border-radius:10px;font-size:13px;font-weight:600;
    }
    .btn-secondary-sm:hover{background:var(--sand);}

    /* partnership / branch / benefit cards */
    .status-pill{
      display:inline-block;font-size:11px;font-weight:700;letter-spacing:.5px;text-transform:uppercase;
      padding:4px 10px;border-radius:20px;
    }
    .status-pending{background:#F3E6C9;color:#8A6A1F;}
    .status-approved{background:#DCEAE0;color:var(--sage-dark);}
    .status-cancelled{background:#F3DCD5;color:var(--danger);}
    .status-inactive{background:#EDEAE3;color:var(--text-mute);}
    .status-active{background:#DCEAE0;color:var(--sage-dark);}

    .info-row{font-size:13px;color:var(--text-dark);display:flex;justify-content:space-between;gap:10px;}
    .info-row span:first-child{color:var(--text-mute);}
    .tag-row{display:flex;flex-wrap:wrap;gap:6px;margin-top:4px;}
    .tag{
      font-size:11px;background:var(--cream);border:1px solid var(--sand-line);
      color:var(--teal-mid);padding:4px 9px;border-radius:20px;
    }

    /* modals: extra widgets */
    .modal-wide{max-width:640px;}
    .checklist{display:flex;flex-direction:column;gap:8px;margin:6px 0 16px;}
    .checklist label{font-size:13.5px;color:var(--text-dark);display:flex;align-items:center;gap:8px;}
    .content-package-row{display:flex;align-items:center;gap:14px;margin-bottom:8px;}
    .content-package-row label{display:flex;align-items:center;gap:8px;font-size:13.5px;min-width:100px;}
    .qty-input{max-width:90px;padding:8px 10px;}
    .qty-input:disabled{opacity:.4;}
    .approve-details{
      background:var(--cream);border:1px solid var(--sand-line);border-radius:10px;
      padding:16px;margin-bottom:16px;font-size:13.5px;
    }
    .approve-details .info-row{margin-bottom:6px;}
    .confirm-row{
      display:flex;align-items:flex-start;gap:10px;font-size:13px;color:var(--text-dark);
      margin-bottom:14px;line-height:1.5;cursor:pointer;
    }
    .confirm-row input{margin-top:3px;}

    /* library content cards */
    .media-card{padding:0;overflow:hidden;}
    .media-thumb{
      width:100%;aspect-ratio:4/3;object-fit:cover;background:var(--sand);display:block;
    }
    .media-video-thumb{
      width:100%;aspect-ratio:4/3;background:var(--teal-deep);color:var(--cream);
      display:flex;align-items:center;justify-content:center;font-size:34px;
      text-decoration:none;
    }
    .media-body{padding:16px;display:flex;flex-direction:column;gap:6px;}
    .media-type-tag{
      position:absolute;margin:10px;font-size:10px;font-weight:700;letter-spacing:.5px;text-transform:uppercase;
      background:rgba(46,74,68,0.8);color:var(--white);padding:3px 9px;border-radius:20px;
    }
    .media-thumb-wrap{position:relative;}

    /* content progress on partnership cards */
    .content-progress{
      font-size:12px;color:var(--text-mute);background:var(--cream);
      border:1px solid var(--sand-line);border-radius:8px;padding:8px 10px;margin-top:4px;
    }
    .content-progress b{color:var(--teal-deep);}
    .upload-drop{
      border:2px dashed var(--sand-line);border-radius:10px;padding:18px;text-align:center;
      background:var(--cream);margin-bottom:6px;
    }
    .upload-drop input[type=file]{width:100%;font-size:13px;}
    .upload-hint{font-size:11.5px;color:var(--text-mute);margin-top:6px;}
    .upload-progress-list{font-size:12.5px;color:var(--text-dark);margin-top:10px;}
    .upload-progress-list div{padding:3px 0;}

    /* AI: stars, mood tags, read-more, multiselect filters */
    .ai-stars{color:var(--gold);font-size:14px;letter-spacing:1px;}
    .ai-stars .star-empty{color:var(--sand-line);}
    .tag-mood{
      font-size:11px;background:#DCEAE0;border:1px solid #BEDCC9;
      color:var(--sage-dark);padding:4px 9px;border-radius:20px;font-weight:600;
    }
    .ai-text-block{margin-top:6px;}
    .ai-text-label{font-size:10.5px;font-weight:700;letter-spacing:.4px;text-transform:uppercase;color:var(--text-mute);margin-bottom:2px;}
    .ai-text-content{
      font-size:12.5px;color:var(--text-dark);line-height:1.5;
      display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;
    }
    .ai-text-content.expanded{display:block;-webkit-line-clamp:unset;overflow:visible;}
    .ai-read-more{
      border:none;background:none;color:var(--sage-dark);font-size:11.5px;font-weight:700;
      padding:2px 0;text-decoration:underline;cursor:pointer;
    }

    .multiselect{position:relative;flex:1;min-width:170px;}
    .multiselect-btn{
      width:100%;padding:12px 14px;border:1px solid var(--sand-line);border-radius:10px;
      background:var(--cream);color:var(--text-dark);text-align:left;font-size:14px;cursor:pointer;
    }
    .multiselect-panel{
      display:none;position:absolute;top:calc(100% + 4px);left:0;right:0;z-index:60;
      background:var(--white);border:1px solid var(--sand-line);border-radius:10px;
      box-shadow:var(--shadow);padding:8px;max-height:230px;overflow-y:auto;
    }
    .multiselect-panel.open{display:block;}
    .multiselect-panel label{
      display:flex;align-items:center;gap:8px;font-size:13px;color:var(--text-dark);
      padding:7px 6px;cursor:pointer;border-radius:6px;
    }
    .multiselect-panel label:hover{background:var(--cream);}

    @media (max-width:760px){
      #appShell.active{flex-direction:column;}
      .sidebar{width:100%;flex-direction:row;overflow-x:auto;padding:14px 0;}
      .sidebar-brand,.sidebar-footer{display:none;}
      .nav-item{border-left:none;border-bottom:3px solid transparent;white-space:nowrap;}
      .nav-item.active{border-bottom-color:var(--gold);}
      .main{padding:22px 18px;}
      .modal-grid{grid-template-columns:1fr;}
    }
  </style>
</head>
<body>

  <!-- ===================== LOGIN SCREEN ===================== -->
  <div id="loginScreen">
    <div class="login-card">
      <div class="brand">ASTOLIA <span>library</span></div>
      <div class="brand-sub">Content &amp; Video Hub</div>

      <div id="loginStep">
        <div class="field-label">Username</div>
        <input type="text" id="usernameInput" class="input" placeholder="Your display name" autocomplete="username">
        <div class="field-label">Password</div>
        <input type="password" id="passwordInput" class="input" placeholder="Your password" autocomplete="current-password">
        <button class="btn" id="loginBtn" onclick="handleLogin()">Sign In</button>
      </div>

      <div class="msg" id="loginMsg"></div>
    </div>
  </div>

  <!-- ===================== APP SHELL ===================== -->
  <div id="appShell">
    <div class="sidebar">
      <div class="sidebar-brand">
        <div class="brand">ASTOLIA <span>library</span></div>
        <div class="brand-sub">Content &amp; Video Hub</div>
      </div>

      <div class="nav-item" data-tab="library" onclick="switchTab('library')">
        <span class="nav-dot"></span> The Library
      </div>
      <div class="nav-item" data-tab="digitalCreator" onclick="switchTab('digitalCreator')">
        <span class="nav-dot"></span> Digital Creator
      </div>
      <div class="nav-item" data-tab="partnerships" onclick="switchTab('partnerships')">
        <span class="nav-dot"></span> Creator Partnerships
      </div>
      <div class="nav-item" data-tab="experiences" onclick="switchTab('experiences')">
        <span class="nav-dot"></span> My Experiences
        <span class="nav-badge hidden" id="expBadge">0</span>
      </div>

      <div class="sidebar-footer">
        <div class="user-chip" id="userChipName">—</div>
        <div class="user-role" id="userChipRole">—</div>
        <div class="logout-link" onclick="handleLogout()">Sign out</div>
      </div>
    </div>

    <div class="main">

      <!-- THE LIBRARY -->
      <div class="tab-page" id="page-library">
        <div class="page-header">
          <div class="page-title">The Library<small>Uploaded content from creator partnerships</small></div>
        </div>

        <div class="filter-bar">
          <select id="libCreatorFilter" onchange="loadLibrary()">
            <option value="">All creators</option>
          </select>
          <select id="libTypeFilter" onchange="loadLibrary()">
            <option value="">All content types</option>
            <option value="Image">Photos</option>
            <option value="Video">Videos</option>
          </select>
          <select id="libBranchFilter" onchange="loadLibrary()">
            <option value="">All spa branches</option>
          </select>
          <select id="libSceneFilter" onchange="loadLibrary()">
            <option value="">All scene types</option>
          </select>

          <div class="multiselect" id="qualityFilterWrap">
            <button type="button" class="multiselect-btn" id="qualityFilterBtn" onclick="toggleMultiselect('qualityFilterPanel')">All quality levels</button>
            <div class="multiselect-panel" id="qualityFilterPanel">
              <label><input type="checkbox" value="5" onchange="onLibraryMultiFilterChange()"> ★★★★★ (5)</label>
              <label><input type="checkbox" value="4" onchange="onLibraryMultiFilterChange()"> ★★★★☆ (4)</label>
              <label><input type="checkbox" value="3" onchange="onLibraryMultiFilterChange()"> ★★★☆☆ (3)</label>
              <label><input type="checkbox" value="2" onchange="onLibraryMultiFilterChange()"> ★★☆☆☆ (2)</label>
              <label><input type="checkbox" value="1" onchange="onLibraryMultiFilterChange()"> ★☆☆☆☆ (1)</label>
            </div>
          </div>

          <div class="multiselect" id="moodFilterWrap">
            <button type="button" class="multiselect-btn" id="moodFilterBtn" onclick="toggleMultiselect('moodFilterPanel')">All moods</button>
            <div class="multiselect-panel" id="moodFilterPanel"></div>
          </div>

          <input type="text" class="input" id="libKeywordsFilter" placeholder="Search keywords..." oninput="debouncedLoadLibrary()">
          <input type="date" class="input" id="libDateFilter" onchange="loadLibrary()">
          <button class="btn-secondary-sm" onclick="clearLibraryFilters()">Clear filters</button>
        </div>

        <div class="grid" id="libraryGrid"></div>
      </div>

      <!-- DIGITAL CREATOR -->
      <div class="tab-page" id="page-digitalCreator">
        <div class="page-header">
          <div class="page-title">Digital Creator<small>Content creator profiles</small></div>
          <button class="btn-accent hidden" id="createProfileBtn" onclick="openCreateModal()">+ Create Profile</button>
        </div>

        <div class="filter-bar" id="filterBar">
          <select id="cityFilter" onchange="loadCreators()">
            <option value="">All cities</option>
          </select>
          <input type="text" class="input" id="searchFilter" placeholder="Search by name..." oninput="debouncedLoadCreators()">
        </div>

        <div class="grid" id="creatorsGrid"></div>
      </div>

      <!-- CREATOR PARTNERSHIPS -->
      <div class="tab-page" id="page-partnerships">
        <div class="page-header">
          <div class="page-title">Creator Partnerships<small>Spa collaborations with content creators</small></div>
          <button class="btn-accent hidden" id="newPartnershipBtn" onclick="openCreatePartnershipModal()">+ New Partnership</button>
        </div>

        <div class="sub-tabs">
          <div class="sub-tab active" data-subtab="list" onclick="switchPartnershipSubTab('list')">Partnerships</div>
          <div class="sub-tab" data-subtab="branches" onclick="switchPartnershipSubTab('branches')">Spa Branches</div>
          <div class="sub-tab" data-subtab="benefits" onclick="switchPartnershipSubTab('benefits')">Benefits Package</div>
        </div>

        <!-- sub: partnerships list -->
        <div class="subtab-page" id="subtab-list">
          <div class="filter-bar">
            <input type="date" class="input" id="pDateFilter" onchange="loadPartnerships()">
            <select id="pStatusFilter" onchange="loadPartnerships()">
              <option value="upcoming">Upcoming</option>
              <option value="past">Past</option>
              <option value="overdue">Overdue</option>
              <option value="cancelled">Cancelled</option>
              <option value="all">All</option>
            </select>
            <button class="btn-secondary-sm" onclick="clearPartnershipDate()">Clear date</button>
          </div>
          <div class="grid" id="partnershipsGrid"></div>
        </div>

        <!-- sub: spa branches -->
        <div class="subtab-page hidden" id="subtab-branches">
          <div class="page-header" style="margin-bottom:16px;">
            <div></div>
            <button class="btn-accent" onclick="openBranchModal()">+ Add Branch</button>
          </div>
          <div class="grid" id="branchesGrid"></div>
        </div>

        <!-- sub: benefits -->
        <div class="subtab-page hidden" id="subtab-benefits">
          <div class="page-header" style="margin-bottom:16px;">
            <div></div>
            <button class="btn-accent" onclick="openBenefitModal()">+ Add Benefit</button>
          </div>
          <div class="grid" id="benefitsGrid"></div>
        </div>
      </div>

      <!-- MY EXPERIENCES -->
      <div class="tab-page" id="page-experiences">
        <div class="page-header">
          <div class="page-title">My Experiences<small>Spa partnerships &amp; approvals</small></div>
        </div>

        <div class="filter-bar">
          <input type="date" class="input" id="eDateFilter" onchange="loadExperiences()">
          <select id="eStatusFilter" onchange="loadExperiences()">
            <option value="upcoming">Upcoming</option>
            <option value="past">Past</option>
            <option value="cancelled">Cancelled</option>
            <option value="all">All</option>
          </select>
          <button class="btn-secondary-sm" onclick="clearExperienceDate()">Clear date</button>
        </div>

        <div class="grid" id="experiencesGrid"></div>
      </div>

    </div>
  </div>

  <!-- ===================== NEW / EDIT PARTNERSHIP MODAL ===================== -->
  <div class="modal-overlay" id="partnershipModal">
    <div class="modal modal-wide">
      <h3 id="partnershipModalTitle">New Partnership</h3>
      <input type="hidden" id="npPartnershipId">

      <div class="field-label">Content creator *</div>
      <select class="input" id="npCreator"></select>

      <div class="modal-grid" style="margin-top:6px;">
        <div>
          <div class="field-label">Spa branch *</div>
          <select class="input" id="npBranch" onchange="onPartnershipBranchChange()"></select>
        </div>
        <div>
          <div class="field-label">Number of people *</div>
          <input type="number" class="input" id="npPeople" min="1" step="1" value="1">
        </div>
        <div>
          <div class="field-label">Visit date *</div>
          <input type="date" class="input" id="npDate">
        </div>
        <div>
          <div class="field-label">Arrival time</div>
          <input type="time" class="input" id="npTime">
        </div>
      </div>

      <div class="field-label">Benefits package — select what's included *</div>
      <div class="checklist" id="npBenefitsList">
        <div class="empty-state" style="padding:20px;">Select a spa branch first.</div>
      </div>

      <div class="field-label">Content package</div>
      <div class="content-package-row">
        <label><input type="checkbox" id="npPostChk" onchange="toggleQtyField('npPostChk','npPostQty')"> Post</label>
        <input type="number" class="input qty-input" id="npPostQty" min="1" step="1" placeholder="Qty" disabled>
      </div>
      <div class="content-package-row">
        <label><input type="checkbox" id="npReelsChk" onchange="toggleQtyField('npReelsChk','npReelsQty')"> Reels</label>
        <input type="number" class="input qty-input" id="npReelsQty" min="1" step="1" placeholder="Qty" disabled>
      </div>
      <div class="content-package-row">
        <label><input type="checkbox" id="npStoryChk" onchange="toggleQtyField('npStoryChk','npStoryQty')"> Story</label>
        <input type="number" class="input qty-input" id="npStoryQty" min="1" step="1" placeholder="Qty" disabled>
      </div>

      <div class="field-label">Upload to platform — raw content required *</div>
      <div class="modal-grid">
        <div>
          <div class="field-label" style="margin-top:0;">Photos to upload</div>
          <input type="number" class="input" id="npRequiredPhotos" min="0" max="20" step="1" value="5">
        </div>
        <div>
          <div class="field-label" style="margin-top:0;">Videos to upload</div>
          <input type="number" class="input" id="npRequiredVideos" min="0" max="20" step="1" value="3">
        </div>
      </div>

      <div class="field-label">Upload deadline *</div>
      <select class="input" id="npDeadline">
        <option value="">Select...</option>
        <option value="Up to 1 week">Up to 1 week</option>
        <option value="Up to 2 weeks">Up to 2 weeks</option>
        <option value="Up to 1 month">Up to 1 month</option>
      </select>

      <div class="msg error" id="npMsg"></div>
      <div class="modal-actions">
        <button class="btn" id="npSaveBtn" onclick="handleSavePartnership()">Send to Creator</button>
        <button class="btn btn-secondary" onclick="closePartnershipModal()">Cancel</button>
      </div>
    </div>
  </div>

  <!-- ===================== BRANCH MODAL ===================== -->
  <div class="modal-overlay" id="branchModal">
    <div class="modal">
      <h3 id="branchModalTitle">Add Spa Branch</h3>
      <input type="hidden" id="branchId">
      <div class="field-label">Branch name *</div>
      <input type="text" class="input" id="brName">
      <div class="field-label">Address</div>
      <input type="text" class="input" id="brAddress">
      <div class="field-label">Operating hours</div>
      <input type="text" class="input" id="brHours" placeholder="e.g. Sun-Thu 9:00-20:00">
      <div class="field-label">Contact person</div>
      <input type="text" class="input" id="brContact">
      <div class="field-label">Activities available</div>
      <div class="checklist">
        <label><input type="checkbox" id="brSwedish"> Swedish Massage</label>
        <label><input type="checkbox" id="brReflex"> Reflexology</label>
        <label><input type="checkbox" id="brPool"> Pool Area</label>
        <label><input type="checkbox" id="brJacuzzi"> Jacuzzi</label>
        <label><input type="checkbox" id="brSauna"> Sauna</label>
      </div>
      <div class="field-label">Status</div>
      <select class="input" id="brStatus">
        <option value="Active">Active</option>
        <option value="Inactive">Inactive</option>
      </select>
      <div class="msg error" id="branchMsg"></div>
      <div class="modal-actions">
        <button class="btn" id="branchSaveBtn" onclick="handleSaveBranch()">Save Branch</button>
        <button class="btn btn-secondary" onclick="closeBranchModal()">Cancel</button>
      </div>
    </div>
  </div>

  <!-- ===================== BENEFIT MODAL ===================== -->
  <div class="modal-overlay" id="benefitModal">
    <div class="modal">
      <h3 id="benefitModalTitle">Add Benefit</h3>
      <input type="hidden" id="benefitId">
      <div class="field-label">Treatment *</div>
      <input type="text" class="input" id="bfTreatment" placeholder="e.g. Swedish Massage">
      <div class="field-label">Duration *</div>
      <input type="text" class="input" id="bfDuration" placeholder="e.g. 60 minutes">
      <div class="field-label">Belongs to activity *</div>
      <select class="input" id="bfActivityType">
        <option value="">Select...</option>
        <option value="Swedish Massage">Swedish Massage</option>
        <option value="Reflexology">Reflexology</option>
        <option value="Pool Area">Pool Area</option>
        <option value="Jacuzzi">Jacuzzi</option>
        <option value="Sauna">Sauna</option>
      </select>
      <div class="field-label">Status</div>
      <select class="input" id="bfStatus">
        <option value="Active">Active</option>
        <option value="Inactive">Inactive</option>
      </select>
      <div class="msg error" id="benefitMsg"></div>
      <div class="modal-actions">
        <button class="btn" id="benefitSaveBtn" onclick="handleSaveBenefit()">Save Benefit</button>
        <button class="btn btn-secondary" onclick="closeBenefitModal()">Cancel</button>
      </div>
    </div>
  </div>

  <!-- ===================== APPROVE PARTNERSHIP MODAL ===================== -->
  <div class="modal-overlay" id="approveModal">
    <div class="modal">
      <h3>Review Partnership</h3>
      <div id="approveDetails" class="approve-details"></div>

      <label class="confirm-row">
        <input type="checkbox" id="apConfirm1">
        I confirm the partnership, the benefits received, and that I will upload the content as agreed.
      </label>

      <label class="confirm-row">
        <input type="checkbox" id="apConfirm2">
        I confirm the general health declaration below.
      </label>
      <div class="field-label">Are you currently pregnant? *</div>
      <select class="input" id="apPregnant">
        <option value="">Select...</option>
        <option value="No">No</option>
        <option value="Yes">Yes</option>
      </select>

      <div class="msg error" id="approveMsg"></div>
      <div class="modal-actions">
        <button class="btn" id="approveSaveBtn" onclick="handleApprovePartnership()">Approve Partnership</button>
        <button class="btn btn-secondary" onclick="closeApproveModal()">Close</button>
      </div>
    </div>
  </div>

  <!-- ===================== UPLOAD CONTENT MODAL ===================== -->
  <div class="modal-overlay" id="uploadModal">
    <div class="modal">
      <h3>Upload Content</h3>
      <div class="content-progress" id="uploadCurrentProgress" style="margin-bottom:14px;"></div>

      <div class="field-label">Photos</div>
      <div class="upload-drop">
        <input type="file" id="uploadImagesInput" accept="image/*" multiple>
        <div class="upload-hint" id="uploadImagesHint">Photos required for this partnership</div>
      </div>

      <div class="field-label">Videos</div>
      <div class="upload-drop">
        <input type="file" id="uploadVideosInput" accept="video/*" multiple>
        <div class="upload-hint" id="uploadVideosHint">Videos required for this partnership. Large videos may take a while to upload.</div>
      </div>

      <div class="upload-progress-list" id="uploadProgressList"></div>
      <div class="msg error" id="uploadMsg"></div>
      <div class="modal-actions">
        <button class="btn" id="uploadSaveBtn" onclick="handleUploadContent()">Upload</button>
        <button class="btn btn-secondary" onclick="closeUploadModal()">Close</button>
      </div>
    </div>
  </div>

  <!-- ===================== EXPERIENCE FEEDBACK MODAL ===================== -->
  <div class="modal-overlay" id="experienceModal">
    <div class="modal">
      <h3>Share Your Experience</h3>
      <input type="hidden" id="expPartnershipId">

      <div class="field-label">How did you feel during the visit? (1 = not great, 5 = amazing) *</div>
      <select class="input" id="expFeeling">
        <option value="">Select...</option>
        <option value="1">1</option><option value="2">2</option><option value="3">3</option><option value="4">4</option><option value="5">5</option>
      </select>

      <div class="field-label">Overall experience (1-5) *</div>
      <select class="input" id="expOverall">
        <option value="">Select...</option>
        <option value="1">1</option><option value="2">2</option><option value="3">3</option><option value="4">4</option><option value="5">5</option>
      </select>

      <div class="field-label">Satisfaction with the spa branch (1-5) *</div>
      <select class="input" id="expSatisfaction">
        <option value="">Select...</option>
        <option value="1">1</option><option value="2">2</option><option value="3">3</option><option value="4">4</option><option value="5">5</option>
      </select>

      <div class="field-label">Tell us more about your experience</div>
      <textarea class="input" id="expNotes" placeholder="Anything you'd like to share..."></textarea>

      <div class="msg error" id="expMsg"></div>
      <div class="modal-actions">
        <button class="btn" id="expSaveBtn" onclick="handleSaveExperience()">Submit</button>
        <button class="btn btn-secondary" onclick="closeExperienceModal()">Close</button>
      </div>
    </div>
  </div>

  <!-- ===================== PROFILE MODAL ===================== -->
  <div class="modal-overlay" id="profileModal">
    <div class="modal">
      <h3 id="modalTitle">Create Profile</h3>
      <input type="hidden" id="profileId">
      <div class="modal-grid">
        <div class="full">
          <div class="field-label">Full name *</div>
          <input type="text" class="input" id="fName" placeholder="Jane Doe">
        </div>
        <div class="full" id="emailFieldWrap">
          <div class="field-label">Email *</div>
          <input type="email" class="input" id="fEmail" placeholder="jane@email.com">
        </div>
        <div>
          <div class="field-label">Phone</div>
          <input type="text" class="input" id="fPhone" placeholder="+1 555 000 0000">
        </div>
        <div>
          <div class="field-label">City</div>
          <input type="text" class="input" id="fCity" placeholder="New York">
        </div>
        <div class="full">
          <div class="field-label">About me</div>
          <textarea class="input" id="fAbout" placeholder="A short bio..."></textarea>
        </div>
        <div class="full">
          <div class="field-label">Instagram link</div>
          <input type="text" class="input" id="fInstagram" placeholder="https://instagram.com/...">
        </div>
        <div class="full">
          <div class="field-label">Facebook link</div>
          <input type="text" class="input" id="fFacebook" placeholder="https://facebook.com/...">
        </div>
        <div class="full">
          <div class="field-label">YouTube link</div>
          <input type="text" class="input" id="fYoutube" placeholder="https://youtube.com/...">
        </div>
        <div class="full">
          <div class="field-label">Status</div>
          <select class="input" id="fStatus">
            <option value="Active">Active</option>
            <option value="Inactive">Inactive</option>
          </select>
        </div>
      </div>
      <div class="msg error" id="modalMsg"></div>
      <div class="modal-actions">
        <button class="btn" id="saveProfileBtn" onclick="handleSaveProfile()">Save Profile</button>
        <button class="btn btn-secondary" onclick="closeModal()">Cancel</button>
      </div>
    </div>
  </div>

  <?!= include('JavaScript'); ?>
</body>
</html>
